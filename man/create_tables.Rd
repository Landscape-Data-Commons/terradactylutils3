% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_files.R
\name{create_tables}
\alias{create_tables}
\title{Create tables in terradactyl expected format}
\usage{
create_tables(
  dsn = NULL,
  csv_path = NULL,
  projectkey,
  path_tables,
  tblPlots_input,
  tblLines_input,
  plots_map,
  lines_map,
  sites_map = NULL,
  tblPlots_input_alt = NULL,
  tblSites_input = NULL,
  tblLPIHeader_input = NULL,
  lpi_header_map = NULL,
  tblLPIDetail_input = NULL,
  lpi_detail_map = NULL,
  tblSpecRichHeader_input = NULL,
  spec_rich_header_map = NULL,
  tblSpecRichDetail_input = NULL,
  spec_rich_detail_map = NULL,
  tblGapHeader_input = NULL,
  gap_header_map = NULL,
  tblGapDetail_input = NULL,
  gap_detail_map = NULL,
  crs_option = "NAD83"
)
}
\arguments{
\item{dsn}{Character. The file path to a spatial database (e.g., `.gdb` folder or folder containing shapefiles). Defaults to `NULL`.}

\item{csv_path}{Character. The file path to a folder containing raw `.csv` input tables. Defaults to `NULL`.}

\item{projectkey}{Character. Unique identifier assigned as the `ProjectKey`, `SpeciesState`, and default fallback tracker across tables.}

\item{path_tables}{Character. The target output directory path where finalized `.csv` tables will be saved.}

\item{tblPlots_input}{Character string name of the plot layer within the DSN/CSV path, or an inline dataframe object.}

\item{tblLines_input}{Character string name of the lines layer within the DSN/CSV path, or an inline dataframe object.}

\item{plots_map}{Named list. Direct mapping of required plot schema attributes to incoming column names (e.g., `list(PrimaryKey = "EvaluationID")`).}

\item{lines_map}{Named list. Direct mapping of required line schema attributes to incoming column names.}

\item{sites_map}{Named list. Optional tracking maps for sites schema attributes. Defaults to `NULL`.}

\item{tblPlots_input_alt}{Optional alternative plot data structures. Defaults to `NULL`.}

\item{tblSites_input}{Optional alternative site data structures or layer names. Defaults to `NULL`.}

\item{tblLPIHeader_input}{Character string layer name or dataframe for LPI header data. Defaults to `NULL`.}

\item{lpi_header_map}{Named list. Column mapping list for the LPI Header table. Defaults to `NULL`.}

\item{tblLPIDetail_input}{Character string layer name or dataframe for LPI point-level detailed data. Defaults to `NULL`.}

\item{lpi_detail_map}{Named list. Column mapping list for LPI point details. Defaults to `NULL`.}

\item{tblSpecRichHeader_input}{Character string layer name or dataframe for Species Richness headers. Defaults to `NULL`.}

\item{spec_rich_header_map}{Named list. Column mapping list for Species Richness headers. Defaults to `NULL`.}

\item{tblSpecRichDetail_input}{Character string layer name or dataframe for Species Richness detailed species counts. Defaults to `NULL`.}

\item{spec_rich_detail_map}{Named list. Column mapping list for Species Richness details. Defaults to `NULL`.}

\item{tblGapHeader_input}{Character string layer name or dataframe for Canopy/Basal Gap intercept headers. Defaults to `NULL`.}

\item{gap_header_map}{Named list. Column mapping list for Gap intercept headers. Defaults to `NULL`.}

\item{tblGapDetail_input}{Character string layer name or dataframe for localized Gap intercept measurements. Defaults to `NULL`.}

\item{gap_detail_map}{Named list. Column mapping list for raw Gap segments. Defaults to `NULL`.}

\item{crs_option}{Character. Spatial coordinate reference system projection choice. Options are `"NAD83"` (EPSG 4269) or `"WGS84"` (EPSG 4326). Defaults to `"NAD83"`.}

\item{DBKey}{the name of the DBKey that will be added to tblPlots}
}
\description{
Reads core vegetation field data methods (LPI, Gap, Species Richness) from a spatial database
(DSN geodatabase/shapefile), CSV files, or standard R dataframes and converts them to the format
needed for terradactyl
}
